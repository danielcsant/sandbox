VAGRANTFILE_API_VERSION = '2'

require 'open-uri'

open('Vagrantfile_global.rb', 'wb') do |file|
  file << open('https://raw.githubusercontent.com/Stratio/sandbox/cont-delivery/Vagrantfile_global.rb').read
end

require_relative 'Vagrantfile_global.rb'

Vfg.new.run

